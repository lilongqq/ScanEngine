# 新检查类型需求分析

## 一、需求清单

| 检查类型 | 描述 | 状态 |
|---|---|---|
| SharePoint 检查 | 实现 SharePoint 的扫描检查 | ✅ 已实现 |
| AWS 对象存储检查 | 实现 AWS 对象存储的检查（加亚马逊云 API） | ✅ 已实现 |
| 南网私有云对象存储检查 | 南网私有云对象存储检查 | ❓ 待确认 |
| HDFS 服务器检查 | HDFS 服务器检查 | ✅ 已实现 |

---

## 二、逐项分析

### ✅ SharePoint 检查 — 已实现，联调完成

**实现文件**：[ScanEngine/crawling/files/sharepointcrawler.py](ScanEngine/crawling/files/sharepointcrawler.py)

**实现方式**：参照 se-8-4 重构，完全自主实现 SOAP 请求，不依赖 shareplum 库

**核心组件**：
- `SOAPHandler`：NTLM 认证 + SOAP 请求封装
  - 使用 `requests` + `HttpNtlmAuth` 处理 NTLM 认证
  - SOAP 请求使用专用 Session；文件下载使用独立 Session（每次调用新建，避免 NTLM 连接状态干扰）
  - 构造并发送 SOAP 请求到 `lists.asmx` 和 `webs.asmx`
  - 支持 `get_list_collection()`、`get_list_items()`、`get_file_content()`
  - 返回原始 XML 响应，由调用方解析

- `SharePointListsHandler`：GetListItems 分页处理
  - 使用 `lxml.etree` 解析 SOAP XML 响应
  - 显式声明 `REQUIRED_FIELDS`（含 `FileSizeDisplay`），确保文件大小字段正确返回
  - SOAP 参数格式：`<viewFields><ViewFields>...</ViewFields></viewFields>`（外层小写为 SOAP 参数名，内层大写为 CAML）
  - `ListItemCollectionPositionNext` 分页书签做 `xml_escape` 转义，防止 `&` 等特殊字符导致 HTTP 400
  - 自动去掉 `ows_` 属性前缀；`FileLeafRef`/`FileRef` 去掉 `ID;#` Lookup 前缀

- `SharePoint`：主 Client 类
  - `get_nodes(path='')`：获取文档库列表（path=''）或指定文件夹内容
  - `get_file(node)`：下载文件内容（独立 NTLM Session，每次新建）
  - `store_file(node, f)`：通过 WebDAV PUT 上传文件到隔离目标路径
  - `empty_file(node)`：通过 WebDAV PUT 空内容置空源文件（占位符模式）
  - `delete_file(node)`：通过 HTTP DELETE 删除源文件
  - 过滤规则：`ServerTemplate == '101'`（精确识别文档库）
  - 文件类型过滤：`FSObjType == '0'`（文件）、`FSObjType == '1'`（文件夹）
  - `_doclib_info` 缓存：`Title → {Name(GUID), RootFolder, ...}`，延迟加载
  - `_get_lib_url_root`：优先用 `RootFolder` 属性，次选 `DefaultViewUrl` 截取 `/Forms/` 前段，兜底用 `'/' + lib_name`
  - GetListItems 传 GUID（`Name` 属性），不传显示名（`Title`）

- `Dict`（UserDict 子类）：Iterator 节点字典
  - `timestamp` → `last_write_time`
  - `diff` → `gen_key(path, last_write_time)`，支持增量检查
  - 正确实现 `__contains__` 和 `__getitem__`，确保 `Differator` 能识别 `'diff' in node`

- `SharePointIterator`：BFS 遍历迭代器
  - 实现 `Iterator` 接口
  - 返回 `(node, partial(get_file, node))` 元组
  - 支持递归遍历文档库及子文件夹

**关键特性**：
- ✅ 不依赖 `Sites.asmx`（SP2010 该接口有 bug），完全从 `lists.asmx` 获取库信息
- ✅ 使用 `requests` + `requests_ntlm` 库处理 NTLM 认证
- ✅ 文件下载与 SOAP 请求使用独立 Session，避免连接池 NTLM 状态混用导致 401
- ✅ `FileSizeDisplay` 显式声明，确保文件大小正确返回（默认视图不含此字段）
- ✅ 支持隔离能力（`store_file` / `empty_file` / `delete_file`，基于 WebDAV）
- ✅ `Dict` 实现 `diff` 字段，支持增量扫描（与 SMB/WPS 保持一致）
- ✅ 分页书签 XML 转义，防止翻页时 HTTP 400

**增量检查说明**：
- `Dict['diff']` = `gen_key(path, last_write_time)`，与 SMB/WPS 保持一致
- `Differator` 读取 Redis set `diff:{identity}:{rules_id}`，判断文件是否已处理
- diff set 由下游数据处理服务在文件命中 DLP 规则并入库后写入；`db_records=0` 时不会写入，增量不生效
- 注意：`filtered by name`/`filtered by size` 日志为正常过滤行为，非报错

**已修复问题（2026-06-22 联调）**：

| 问题 | 根因 | 修复方式 |
|------|------|------|
| `size` 全部为 0 / `filtered by size` | `FileSizeDisplay` 未在 ViewFields 声明，SP 不默认返回 | 加入 `REQUIRED_FIELDS`，SOAP 参数用 `<viewFields><ViewFields>` 双层结构 |
| 文件下载 HTTP 401 | SOAP Session 与文件 GET 共用连接池，NTLM 握手状态互相干扰 | `get_file_content` 改用每次调用独立新建的 Session |
| 翻页 HTTP 400 | 分页书签含 `&` 等 XML 特殊字符，直接拼入 SOAP XML 破坏结构 | 对 `next_bookmark` 调用 `xml_escape` 转义 |
| 增量检查不生效 | `Dict` 未实现 `__contains__`/`__getitem__` 对 `'diff'` 的处理，`'diff' in node` 永远为 `False` | 补充 `diff` 的 `__contains__` 和 `__getitem__`，使用 `gen_key(path, last_write_time)` |
| 隔离能力缺失 | 只有 `get_file`，无写入/删除方法 | 新增 `store_file`/`empty_file`/`delete_file`（基于 WebDAV PUT/DELETE）|

**支持版本**：2007 / 2010 / 2013 / 2016 / 2019 / O365

**依赖库**：
```
requests
requests-ntlm
lxml
python-dateutil
```

**classmap 注册**：
- protocol：`'SHAREPOINT': 'crawling.files.sharepointcrawler.SharePointOne'`
- iterator：`'SHAREPOINT': 'crawling.files.sharepointcrawler.SharePointIterator'`

**结论**：完整实现，2026-06-22 联调通过，可正常扫描 SharePoint 2010 文档库，隔离能力已具备。

---

### ✅ AWS 对象存储检查 — 已实现

**实现文件**：[ScanEngine/crawling/files/osscrawler.py](ScanEngine/crawling/files/osscrawler.py)

**实现类**：`AWSCloud`（基类）、`AWSCloudBatch`（iterator client）、`AWSCloudOne`（单例 client，`@weak_cache`）

**关键参数**：`ak`、`sk`、`region`、`verify=True`，`endpoint=None`（AWS 无需自定义 endpoint）

**classmap 注册**：
- protocol：`'AWS': 'crawling.files.osscrawler.AWSCloudOne'`
- iterator：`'AWS': 'crawling.files.osscrawler.OSSIterator'`

**结论**：完整实现，已注册，直接可用。

---

### ❓ 南网私有云对象存储检查 — 待确认协议类型

**背景**：代码库中无任何南网相关实现。

**关键问题**：南网私有云对象存储使用什么协议？

| 情况 | 判断依据 | 工作量 |
|---|---|---|
| **S3 兼容协议** | 提供了 endpoint URL、AK/SK，类似浪潮云/金山云 | 极小：在 `factory` 和 `cloud_map` 各加一行 `'NanwangCloud': S3One`，无需新增类 |
| **OpenStack Swift** | 提供了 auth URL + tenant/project | 中等：新增 `SwiftCloud` 类，使用 `swiftclient` 库 |
| **专有 API** | 有单独的接口文档 | 较大：参照 `HuaWeiCloud` 从头开发 |

**待办**：
- [ ] 向南网侧索取对象存储接口文档或确认协议类型
- [ ] 确认认证方式（AK/SK、Token、OAuth2 等）
- [ ] 确认是否有 Python SDK

---

### ✅ HDFS 服务器检查 — 已实现，无需开发

**实现文件**：[ScanEngine/crawling/files/hdfscrawler.py](ScanEngine/crawling/files/hdfscrawler.py)

**使用库**：`ex3rd.hdfs.SecureClient`（支持 Kerberos 认证）

**classmap 注册**：`'HDFS': 'crawling.files.hdfscrawler.HDFSOne'`（client）、`'HDFS': 'crawling.files.hdfscrawler.HDFSIterator'`（iterator）

**结论**：完整实现，直接可用。

---

## 三、待办事项

| 优先级 | 任务 | 负责 |
|---|---|---|
| 🟡 中 | 向南网侧确认对象存储协议类型及接口文档 | 对接 |
| 🟡 中 | 根据南网协议类型决定开发方案（S3兼容/新增类） | 开发 |

---

## 四、相关文件

- [ScanEngine/crawling/files/osscrawler.py](ScanEngine/crawling/files/osscrawler.py) — 对象存储（阿里云/华为/腾讯/S3兼容），AWS 新类将在此文件新增
- [ScanEngine/crawling/files/sharepointcrawler.py](ScanEngine/crawling/files/sharepointcrawler.py) — SharePoint 实现
- [ScanEngine/crawling/files/hdfscrawler.py](ScanEngine/crawling/files/hdfscrawler.py) — HDFS 实现
- [ScanEngine/common/classmap.py](ScanEngine/common/classmap.py) — 类型注册表，新增类后需在此注册
